{
  "name": "bbcat-orchestration-builder",
  "private": true,
  "scripts": {
    "dev": "(cd electron-app && yarn dev)",
    "debug": "(cd electron-app && yarn debug)",
    "build": "(cd electron-app && yarn build)",
    "dist": "(cd electron-app && yarn dist)",
    "bump": "node scripts/bump.js && git add */package.json",
    "postinstall": "(cd logging && yarn install) && (cd background-tasks && yarn install) && (cd react-frontend && yarn install) && (cd electron-app && yarn install)",
    "lint": "(cd background-tasks && yarn lint) && (cd electron-app && yarn lint) && (cd react-frontend && yarn lint) && (cd logging && yarn lint)",
    "test": "(cd background-tasks && yarn test) && (cd electron-app && yarn test) && (cd react-frontend && yarn test) && (cd logging && yarn test)",
    "credits": "(cd electron-app && yarn credits) && git add electron-app/credits.html"
  },
  "packages": [
    "background-tasks",
    "electron-app",
    "react-frontend",
    "logging"
  ],
  "version": "0.0.0",
  "devDependencies": {
    "lefthook": "1.9.2"
  },
  "dependencies": {}
}
