{
  "name": "bbcat-orchestration-builder",
  "private": true,
  "scripts": {
    "dev": "(cd electron-app && yarn dev)",
    "build": "(cd electron-app && yarn build)",
    "dist": "(cd electron-app && yarn dist)",
    "bump": "node scripts/version.js"
  },
  "version": "0.0.0",
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
